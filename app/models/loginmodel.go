// app/models/model_example.go
package models

// Example model (can be expanded based on your application's data model)
type ExampleModel struct {
	ID   int    `json:"id"`
	Name string `json:"name"`
}
